{
  "name": "stripe-subscription-with-fixed-price",
  "version": "1.0.0",
  "description": "How to create a fixed price monthly subscription and sign up form.",
  "main": "server.js",
  "scripts": {
    "start": "node ./server/node/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "stripe-demos",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "dotenv": "^8.0.0",
    "express": "^4.18.2",
    "stripe": "^7.4.0"
  }
}
